generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}
model Feedback {
  id        BigInt   @id @default(autoincrement())
  name      String   @unique
  company   String?
  comment   String?
  avatar    String?
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz(6)

  @@map("feedbacks")
}

model Project {
  id          BigInt   @id @default(autoincrement())
  title       String   @unique
  category    String
  description String
  client      String?
  imageUrl    String   @map("image_url")
  createdAt   DateTime @default(now()) @map("created_at") @db.Timestamptz(6)

  @@map("projects")
}

model TimelineExperience {
  id         BigInt           @id @default(autoincrement())
  startYear  Int              @map("start_year")
  endYear    Int?             @map("end_year")
  isCurrent  Boolean          @default(false) @map("is_current")
  role       String
  company    String
  description String
  createdAt  DateTime         @default(now()) @map("created_at") @db.Timestamptz(6)

  gallery    TimelineGallery[]

  @@map("timeline_experiences")
}

model TimelineGallery {
  id           BigInt            @id @default(autoincrement())
  experienceId BigInt            @map("experience_id")
  imageUrl     String            @map("image_url")
  createdAt    DateTime          @default(now()) @map("created_at") @db.Timestamptz(6)

  experience   TimelineExperience @relation(fields: [experienceId], references: [id], onDelete: Cascade)

  @@map("timeline_gallery")
}
